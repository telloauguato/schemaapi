# Number

The `number` type is designed to represent numeric values, useful in various data modeling scenarios.

import { Callout } from 'nextra/components'

<Callout type="info" emoji="ℹ️">
 The **Inherited Property** of this type is [Number](/inherited/number)
</Callout>

import { Tabs } from 'nextra/components'

## Attribution

<Tabs items={['Direct', 'Incorporated', 'Return']}>
    <Tabs.Tab>
    ```json
    {
        "key": "number"
    }
    ```
    </Tabs.Tab>
    <Tabs.Tab>
    ```json
    {
        "key": {
            "type": "number"
        }
    }
    ```
    </Tabs.Tab>
    <Tabs.Tab>
    ```json
    {
        "key": 1520
    }
    ```
    </Tabs.Tab>
</Tabs>



## Properties

| Name | Type | Description | Required |
| ------------- | -------- | -------------- | --------- |
| `type` | `string` | Type of data that will be returned. Must be assigned as `number`. | Yes |